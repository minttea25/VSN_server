namespace VSN;

// Client -> Server
table PlayerUpgrade {
  player_nid: uint; // network id of the player
  skill_type_id: uint; // the type id of the skill (passive and active)
  value: uint; // the value of the upgrades
}

root_type PlayerUpgrade;