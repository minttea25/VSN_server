include "Types.fbs";

namespace VSN;

struct DamagedMonsterInfo {
  nid : uint; // network id of the monster
  damage: short; // damage value
  dead: bool; // if the monster is dead
}

// Server <-> Client
table MonsterDamaged {
  infos: [DamagedMonsterInfo]; // list of damaged monsters
}


root_type MonsterDamaged;